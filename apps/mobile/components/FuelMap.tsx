import {Pressable,Text,View} from 'react-native';import type {NearbyPrice} from '../../../packages/types/src';
export function FuelMap({prices,onSelect}:{latitude:number;longitude:number;prices:NearbyPrice[];onSelect?:(price:NearbyPrice)=>void}){return <View style={{minHeight:180,alignItems:'center',justifyContent:'center',backgroundColor:'#D4E2DF',borderRadius:16}}><Text>{prices.length} nearby price markers · map available on iOS/Android</Text>{prices.map(price=><Pressable key={price.station.id} onPress={()=>onSelect?.(price)}><Text>{price.station.name} · ${price.price}/L · View</Text></Pressable>)}</View>}
