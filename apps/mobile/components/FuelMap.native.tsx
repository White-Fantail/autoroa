import MapView,{Marker} from 'react-native-maps';import type {NearbyPrice} from '../../../packages/types/src';import {freshness} from '../../../packages/config/src';
export function FuelMap({latitude,longitude,prices,onSelect}:{latitude:number;longitude:number;prices:NearbyPrice[];onSelect?:(price:NearbyPrice)=>void}){return <MapView style={{height:280,borderRadius:16}} initialRegion={{latitude,longitude,latitudeDelta:.12,longitudeDelta:.12}}>{prices.map(item=><Marker key={item.station.id} coordinate={{latitude:Number(item.station.latitude),longitude:Number(item.station.longitude)}} title={`${item.station.name} · $${item.price}/L`} description={`${freshness(item.observed_at)} · ${item.verification_level}`} onPress={()=>onSelect?.(item)}/>)}</MapView>}
