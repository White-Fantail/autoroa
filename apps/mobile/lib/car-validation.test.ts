import {describe,expect,it} from 'vitest';import {fillEditSchema,selectedStation,selectStation,vehicleEditSchema} from './car-validation';
describe('Car editors',()=>{it('rejects blank required fill-up values',()=>{expect(fillEditSchema.safeParse({station_id:'',occurred_at:'',fuel_type:'PETROL_91',litres:'',pump_price_per_litre:'',discount_amount:'0',total_amount:'',odometer_km:'',notes:'',full_tank:true,missed_previous_fill:false}).success).toBe(false)});it('converts valid numeric payloads',()=>{const value=fillEditSchema.parse({station_id:'',occurred_at:'2026-01-01T00:00:00.000Z',fuel_type:'DIESEL',litres:'40',pump_price_per_litre:'2.2',discount_amount:'1',total_amount:'87',odometer_km:'80000',notes:'',full_tank:true,missed_previous_fill:false});expect(value.litres).toBe(40);expect(value.station_id).toBeNull()});it('accepts API nulls on optional vehicle and fill-up fields',()=>{expect(vehicleEditSchema.parse({nickname:'Car',make:'A',model:'B',year:null,variant:null,fuel_type:'PETROL_91',registration_plate:null,tank_capacity_litres:null}).year).toBeNull();const fill=fillEditSchema.parse({station_id:null,occurred_at:'2026-01-01T00:00:00.000Z',fuel_type:'PETROL_91',litres:'40',pump_price_per_litre:null,discount_amount:null,total_amount:'80',odometer_km:'12',notes:null,full_tank:true,missed_previous_fill:false});expect(fill.pump_price_per_litre).toBeNull();expect(fill.notes).toBe('')});it('selects and clears station controls',()=>{expect(selectStation({id:'station-1'})).toBe('station-1');expect(selectStation(undefined)).toBeNull()});it('restores the selected station and prefers a newly searched result',()=>{const saved={id:'station-1',name:'Saved'};const replacement={id:'station-2',name:'Replacement'};expect(selectedStation('station-1',saved,[replacement])).toBe(saved);expect(selectedStation('station-2',saved,[replacement])).toBe(replacement);expect(selectedStation(null,saved,[replacement])).toBeUndefined()});it('retains a newly selected station after later search results replace its list',()=>{const chosen={id:'station-2',name:'Chosen'};const laterResults=[{id:'station-3',name:'Another'}];expect(selectedStation('station-2',chosen,laterResults)).toBe(chosen)});it('validates vehicle year and fuel',()=>{expect(vehicleEditSchema.safeParse({nickname:'Car',make:'A',model:'B',year:'1800',variant:'',fuel_type:'INVALID',registration_plate:'',tank_capacity_litres:''}).success).toBe(false)})});
