import {useState} from 'react';
export function useReviewState(initialFull=true){const [full,setFull]=useState(initialFull);const [saving,setSaving]=useState(false);const [error,setError]=useState<string>();const [saved,setSaved]=useState(false);async function submit(valid:boolean,operation:()=>Promise<void>){if(saving)return false;if(!valid){setError('Review invalid fields.');return false}setSaving(true);setError(undefined);try{await operation();setSaved(true);return true}catch(error){setError(error instanceof Error?error.message:'Save failed. Your form is preserved for retry.');return false}finally{setSaving(false)}}return {full,setFull,saving,error,setError,saved,submit}}
